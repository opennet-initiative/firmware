#!/bin/sh
#
# Ueberblick ueber Firewall-Einstellungen
#

set -eu


# aktuell wirksame Firewall-Regeln
nft list tables | while read -r table; do	
    filename="${table// /_}"
    nft list $table >"${filename}_rule.nft"
done


# uci-Konfiguration
uci -q show "firewall" >uci_firewall || true
