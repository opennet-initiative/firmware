#!/bin/sh
#
# Ueberblick ueber Firewall-Einstellungen
#

set -eu


# aktuell wirksame Firewall-Regeln
for table in filter nat mangle; do
	iptables -t "$table" -L -vn >"iptables_${table}.rules"
done


# uci-Konfiguration
uci -q show "firewall" >uci_firewall || true
