Allow switching to DFS channel after radar detection. See:
https://w1.fi/cgit/hostap/commit/src/ap/dfs.c?id=04f667fcdde158c1baf8d99516c96d21b878b919

--- /dev/null
+++ b/openwrt/package/network/services/hostapd/patches/499-dfs-ETSI.patch
@@ -0,0 +1,14 @@
+--- a/src/ap/dfs.c
++++ b/src/ap/dfs.c
+@@ -839,6 +839,11 @@
+ 	/* Check if active CAC */
+ 	if (iface->cac_started)
+ 		return hostapd_dfs_start_channel_switch_cac(iface);
++	/*
++	 * Allow selection of DFS channel in ETSI to comply with
++	 * uniform spreading.
++	 */
++	skip_radar = 0;
+ 
+ 	/* Perform channel switch/CSA */
+ 	channel = dfs_get_valid_channel(iface, &secondary_channel,
